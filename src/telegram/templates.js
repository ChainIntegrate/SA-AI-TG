export async function tgSend(cfg, chatId, text) {
  // Placeholder: qui andrai a chiamare Telegram sendMessage
  // Io lo lascio minimal perché dipende se usi fetch, axios, o libreria.
  console.log(`[tgSend] chat=${chatId}\n${text}\n`);
}
